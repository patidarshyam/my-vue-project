<template>
  <div class="card">
    <div class="card-header">
      <button class="accordion-toggle" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
        Employee Search
        <i class="bi bi-chevron-up"></i>
      </button>
    </div>

    <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
      <div class="card-body">
        <div class="row">
          <div class="col-md-6">
            <div class="form-floating">
              <input id="lastName" v-model="searchCriteria.lastName" class="form-control form-control-sm custom-input" />
              <label for="lastName">Last Name</label>
            </div>
            <div class="form-floating">
              <input id="firstName" v-model="searchCriteria.firstName" class="form-control form-control-sm custom-input" />
              <label for="firstName">First Name</label>
            </div>
          </div>
          <div class="col-md-6">
            <div class="form-floating">
              <input id="employeeNumber" v-model="searchCriteria.employeeNumber" class="form-control form-control-sm custom-input" />
              <label for="employeeNumber">Gems ID</label>
            </div>
            <div class="form-floating" style="float: left; margin-top: 5px">
              <button type="button" @click="searchEmployee" :disabled="isDisabled" class="px-4 btn btn-outline-primary">Search</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.card {
  border: 1px solid #ccc;
  border-radius: 5px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.3);
  margin-bottom: 20px;
}

.card-header {
  background-color: #f5f5f5;
  border-bottom: 1px solid #ddd;
  padding: 8px 16px;
  cursor: pointer;
}

.card-body {
  padding: 16px;
}

.accordion-toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: none;
  border: none;
  font-size: 18px;
  font-weight: bold;
  color: #007bff;
}

.custom-input {
  font-size: 14px;
  padding: 7px;
}

.btn-outline-primary {
  font-size: 14px;
  padding: 8px 12px;
}

/* Add more custom styles as needed */
</style>
=================
<template>
  <div class="card">
    <div class="card-header">
      <button class="accordion-toggle" type="button" data-bs-toggle="collapse" data-bs-target="#collapseOne" aria-expanded="true" aria-controls="collapseOne">
        Employee Search
        <i class="bi bi-chevron-up"></i>
      </button>
    </div>

    <div id="collapseOne" class="collapse show" aria-labelledby="headingOne" data-bs-parent="#accordionExample">
      <div class="card-body">
        <div class="row">
          <div class="col-md-6">
            <div class="form-floating">
              <input id="lastName" v-model="searchCriteria.lastName" class="form-control form-control-sm custom-input" />
              <label for="lastName">Last Name</label>
            </div>
            <div class="form-floating">
              <input id="firstName" v-model="searchCriteria.firstName" class="form-control form-control-sm custom-input" />
              <label for="firstName">First Name</label>
            </div>
          </div>
          <div class="col-md-6">
            <div class="form-floating">
              <input id="employeeNumber" v-model="searchCriteria.employeeNumber" class="form-control form-control-sm custom-input" />
              <label for="employeeNumber">Gems ID</label>
            </div>
            <div class="form-floating" style="float: left; margin-top: 5px">
              <button type="button" @click="searchEmployee" :disabled="isDisabled" class="px-4 btn btn-outline-primary">Search</button>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</template>

<style scoped>
.card {
  border: 1px solid #ccc;
  border-radius: 5px;
  box-shadow: 0 2px 6px rgba(0, 0, 0, 0.3);
  margin-bottom: 20px;
}

.card-header {
  background-color: #f5f5f5;
  border-bottom: 1px solid #ddd;
  padding: 8px 16px;
  cursor: pointer;
}

.card-body {
  padding: 16px;
}

.accordion-toggle {
  display: flex;
  align-items: center;
  justify-content: space-between;
  background: none;
  border: none;
  font-size: 18px;
  font-weight: bold;
  color: #007bff;
}

.custom-input {
  font-size: 14px;
  padding: 7px;
}

.btn-outline-primary {
  font-size: 14px;
  padding: 8px 12px;
}

/* Responsive Styles */
@media (max-width: 768px) {
  /* For tablet devices, adjust the input font-size and padding */
  .custom-input {
    font-size: 12px;
    padding: 6px;
  }

  /* Adjust button font-size and padding */
  .btn-outline-primary {
    font-size: 12px;
    padding: 6px 10px;
  }
}

/* Add more custom styles as needed */
</style>
